import en from '../locales/en.json'
import kr from '../locales/kr.json'
export default {
  locale: 'en',
  fallbackLocale: 'en',
  // messages: { en, kr }
  messages: {
    "en" : en,
    "kr" : kr,
  },
}
